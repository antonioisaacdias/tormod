import type { Tone } from '@/types/tone'

export const toneText: Record<Tone, string> = {
  safe: 'text-safe',
  approve: 'text-approve',
  arc: 'text-arc',
  danger: 'text-danger',
  faint: 'text-faint',
}
