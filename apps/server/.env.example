# Bearer token — the server refuses to start without it.
# Generate one with: openssl rand -hex 32
TORMOD_TOKEN=replace-with-a-long-random-secret

# Listening port (default 8790).
PORT=8790

# Path to the append-only SQLite audit file (default tormod-audit.db).
TORMOD_AUDIT=./tormod-audit.db
